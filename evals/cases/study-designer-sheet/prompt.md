Make the Sheet an iOS-style bottom sheet: it peeks at the bottom (its top edge at y 700), you drag it up to open it (top edge at y 120), and flick it down to close it back to the peek. It snaps between the two with springs and follows the finger while you drag. Dim the background with the Dim layer: opacity 0.4 when the sheet is open, 0 when it peeks. Verify it with a simulated drag.
